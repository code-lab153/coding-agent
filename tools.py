"""Local tool layer skeleton.

This module will later define schemas, validation, registration, and local
handlers for list_files, read_file, write_file, and run_command. It must not
call the model or manage conversation history.
"""
